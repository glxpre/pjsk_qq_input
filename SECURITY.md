# 安全政策 (Security Policy)

## 🔒 支持的版本

我们为以下版本提供安全更新：

| 版本 | 支持状态 |
| --- | --- |
| 最新版本 | ✅ 支持 |
| 旧版本 | ❌ 不支持 |

我们建议始终使用最新版本以获得最佳安全性。

## 🐛 报告漏洞

如果你发现了安全漏洞，**请不要公开披露**。我们非常重视安全问题，并会尽快处理。

### 报告方式

**推荐方式：GitHub Security Advisories**

1. 访问项目的 Security 标签
2. 点击 "Report a vulnerability"
3. 填写漏洞详情
4. 提交报告

**备用方式：私密 Issue**

如果无法使用 Security Advisories，可以：
1. 创建一个 Issue
2. 标题使用 `[SECURITY]` 前缀
3. 在描述中说明这是安全问题
4. 我们会尽快将其转为私密讨论

### 报告内容

请在报告中包含以下信息：

- **漏洞类型**：XSS、注入攻击、CSRF 等
- **影响范围**：哪些功能/组件受影响
- **复现步骤**：详细的步骤说明
- **影响评估**：可能造成的危害
- **修复建议**：如果有的话
- **环境信息**：浏览器、操作系统等

### 示例报告

```markdown
## 漏洞类型
XSS (跨站脚本攻击)

## 影响范围
贴纸文本输入功能

## 复现步骤
1. 打开贴纸生成器
2. 在文本框输入：`<script>alert('XSS')</script>`
3. 生成贴纸时执行脚本

## 影响评估
攻击者可以窃取用户 Cookie 或执行恶意操作

## 修复建议
对用户输入进行 HTML 转义处理
```

## ⏱️ 响应时间

- **初步响应**：48 小时内
- **漏洞确认**：7 天内
- **修复发布**：根据严重程度，14-30 天内

## 🎖️ 致谢

我们会在修复发布后公开致谢报告者（除非你要求匿名）。

感谢以下安全研究人员的贡献：
- （待添加）

## 🔐 安全最佳实践

### 对于用户

1. **保持更新**：使用最新版本
2. **HTTPS**：确保使用 HTTPS 访问
3. **警惕钓鱼**：不要点击可疑链接
4. **浏览器安全**：使用最新版本的浏览器

### 对于开发者

1. **输入验证**：验证所有用户输入
2. **输出转义**：转义所有输出到 HTML 的内容
3. **依赖更新**：定期更新依赖包
4. **代码审查**：所有代码都需要审查
5. **CSP 配置**：配置内容安全策略

## 🛡️ 安全功能

本项目实施的安全措施：

- ✅ HTTPS 强制（生产环境）
- ✅ 输入验证
- ✅ XSS 防护
- ✅ 客户端渲染安全
- ✅ 安全的文件上传（限制文件类型和大小）
- ✅ 无后端数据存储（客户端应用）

### 桌面版（QQ 贴纸伴侣）额外说明

桌面版会读取你在 QQ 中**显式选中**的文字，因此这里把边界写清楚：

- ✅ **不注入 QQ 进程、不修改 QQ 文件、不使用任何非官方协议**；
  QQ 相关的操作只有两件事：通过系统剪贴板读一次复制结果、向当前焦点窗口发送一次 Ctrl+V。
- ✅ **只在按下快捷键时合成一次 Ctrl+C / Ctrl+V**，不安装全局键盘钩子、
  不记录按键、不持续模拟按键。
- ✅ **不记录、不上传任何聊天内容**；默认连生成历史都不写盘，
  打开后也只保存在本机用户目录（`recordHistory`）。
- ✅ 渲染进程开启 `contextIsolation`、关闭 `nodeIntegration`，
  预加载脚本只暴露白名单 API；页面 CSP 禁止任何远程加载，安装后完全离线。
- ✅ 读取选区前会先确认剪贴板**确实发生了变化**，避免误用剪贴板里的旧内容；
  插入前会重新确认 QQ 聊天输入框仍是当前焦点，否则只复制图片、不发送粘贴。
- ✅ **从不自动发送消息**：程序不接管回车键，发不发由你决定。

## 📚 相关资源

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Web Security Cheat Sheet](https://cheatsheetseries.owasp.org/)
- [React Security Best Practices](https://reactjs.org/docs/dom-elements.html#dangerouslysetinnerhtml)

## 📧 联系方式

如有其他安全相关问题，可以通过以下方式联系：

- GitHub Issues（非敏感问题）
- Security Advisories（敏感问题）

---

感谢你帮助我们保持项目的安全！🙏

<div align="center">

**安全是我们的首要任务**

基于 [25-ji-code-de/stickers-maker](https://github.com/25-ji-code-de/stickers-maker) 二次开发

Made with 💜

</div>
